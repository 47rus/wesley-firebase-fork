# Firebase Migration

This directory contains the script to migrate data from Supabase to Firebase.

## Steps to migrate

1.  **Install dependencies:**
    ```bash
    npm install
    ```
2.  **Run the migration script:**
    ```bash
    npm run migrate
    ```
