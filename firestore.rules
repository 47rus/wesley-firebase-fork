rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access for all documents
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null; // Optional: Require authentication for writes
    }
  }
}
